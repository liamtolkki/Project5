using namespace std;
#include "Trie.h"

Trie::Trie()
{ // make a new trie with a root node (Empty)
    root = new Node();
}

bool Trie::insert(string str)
{ // inserts a string
}

int Trie::count()
{
    return count;
}
int Trie::getSize()
{
    return size;
}